diff --git a/docker-compose.env.example b/docker-compose.env.example
index b745181..dcd45e3 100644
--- a/docker-compose.env.example
+++ b/docker-compose.env.example
@@ -21,3 +21,27 @@ MINIO_BUCKET_NAME=tailchat
 LIVEKIT_URL=http://livekit:7880
 LIVEKIT_PUBLIC_URL=wss://chat.example.com/livekit
 LIVEKIT_KEYS=CHANGE_ME_KEY:CHANGE_ME_SECRET_32CHARS_MIN
+
+# ----------------------------
+# 生产加固（建议启用）
+# ----------------------------
+
+# Traefik：仅信任你自己的反代转发头（逗号分隔）
+# 示例：TRAEFIK_TRUSTED_PROXIES=127.0.0.1/32,10.0.0.0/8
+TRAEFIK_TRUSTED_PROXIES=127.0.0.1/32
+
+# Socket.IO CORS：生产环境建议收敛到站点域名（逗号分隔）
+# 示例：SOCKETIO_CORS_ORIGINS=https://goodspage.cn
+SOCKETIO_CORS_ORIGINS=https://chat.example.com
+
+# OIDC：生产默认建议开启 PKCE（true/false）
+OIDC_REQUIRE_PKCE=true
+
+# 未捕获异常：生产默认建议退出自愈（true/false）
+EXIT_ON_UNCAUGHT=true
+
+# Defense Control：生产必须配置（用于 executor HMAC 校验）
+DEFENSE_SHARED_SECRET=CHANGE_ME_LONG_RANDOM
+
+# 可观测性：开启 Prometheus metrics（true/false）
+PROMETHEUS=false
diff --git a/docker-compose.yml b/docker-compose.yml
index 111ea5b..5eb961d 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -20,6 +20,9 @@ services:
     environment:
       SERVICES: core/gateway,core/user/*.service.js,core/group/*.service.js,core/chat/*.service.js,core/file,core/plugin/registry,core/config
       PORT: 3000
+    # 可观测性：Prometheus metrics（仅本机暴露，需配合 docker-compose.env: PROMETHEUS=true）
+    ports:
+      - "127.0.0.1:13030:13030"
     labels:
       - "traefik.enable=true"
       - "traefik.http.routers.api-gw.rule=PathPrefix(`/`)"
@@ -113,7 +116,10 @@ services:
       - "--providers.docker=true"
       - "--providers.docker.exposedbydefault=false"
       - "--entryPoints.web.address=:80"
-      - "--entryPoints.web.forwardedHeaders.insecure" # Not good
+      # 生产建议：只信任你自己的反代（默认仅 localhost）
+      # 如使用 Cloudflare/Nginx 等，可在 docker-compose.env 中设置：
+      #   TRAEFIK_TRUSTED_PROXIES=127.0.0.1/32,10.0.0.0/8
+      - "--entryPoints.web.forwardedHeaders.trustedIPs=${TRAEFIK_TRUSTED_PROXIES:-127.0.0.1/32}"
     ports:
       - 11000:80
       - 127.0.0.1:11001:8080
diff --git a/scripts/env-lint.sh b/scripts/env-lint.sh
index 7e5e495..7d2c659 100755
--- a/scripts/env-lint.sh
+++ b/scripts/env-lint.sh
@@ -12,11 +12,33 @@ fi
 if grep -Fq '`' "$ENV_FILE" || grep -Fq '｀' "$ENV_FILE"; then
   echo "❌ ERROR: Found backticks (\` or ｀) in $ENV_FILE!"
   echo "   This will break shell parsing / env parsing and cause extremely confusing runtime bugs."
-  echo "   Fix: remove them (recommended):"
+  echo "   Fix (recommended):"
+  echo "        bash scripts/fix-env.sh $ENV_FILE"
+  echo "   Or remove them manually:"
   echo "        perl -pi -e \"s/\\x60//g; s/｀//g\" $ENV_FILE"
   exit 1
 fi

+# 检查是否有意外的引号包裹（例如 API_URL=\"https://...\" 或 API_URL='https://...'）
+check_wrapped_value() {
+  local key="$1"
+  local val
+  val="$(awk -F= -v k="$key" '$1==k{print $2}' "$ENV_FILE" | tail -n 1 || true)"
+  if [ -z "${val:-}" ]; then
+    return 0
+  fi
+  if echo "$val" | grep -Eq "^[\"'].*[\"']$"; then
+    echo "❌ ERROR: $key should NOT be wrapped by quotes."
+    echo "   Current: $key=$val"
+    echo "   Fix: remove the wrapping quotes (or run: bash scripts/fix-env.sh $ENV_FILE)"
+    exit 1
+  fi
+}
+
+check_wrapped_value "API_URL"
+check_wrapped_value "LIVEKIT_URL"
+check_wrapped_value "MINIO_URL"
+
 # MINIO_URL 只能是 host:port（例如 minio:9000），不能带 http(s)://
 MINIO_URL="$(awk -F= '/^MINIO_URL=/{print $2}' "$ENV_FILE" | tail -n 1 || true)"
 if [ -n "${MINIO_URL:-}" ]; then
@@ -42,5 +64,19 @@ if [ -n "${API_URL:-}" ] && ! echo "$API_URL" | grep -Eqi '^https?://'; then
   exit 1
 fi

+# LIVEKIT_URL（容器内地址）应为 http://livekit:7880 这种可访问地址
+LIVEKIT_URL="$(awk -F= '/^LIVEKIT_URL=/{print $2}' "$ENV_FILE" | tail -n 1 || true)"
+if [ -n "${LIVEKIT_URL:-}" ] && ! echo "$LIVEKIT_URL" | grep -Eqi '^https?://'; then
+  echo "❌ ERROR: LIVEKIT_URL must start with http:// or https://"
+  echo "   Current: LIVEKIT_URL=$LIVEKIT_URL"
+  exit 1
+fi
+
+# MINIO_BUCKET_NAME 建议显式配置，避免运行时出现 undefined bucket
+MINIO_BUCKET_NAME="$(awk -F= '/^MINIO_BUCKET_NAME=/{print $2}' "$ENV_FILE" | tail -n 1 || true)"
+if [ -z "${MINIO_BUCKET_NAME:-}" ]; then
+  echo "⚠️ WARNING: MINIO_BUCKET_NAME is empty. Recommended: MINIO_BUCKET_NAME=tailchat"
+fi
+
 echo "✅ env-lint: $ENV_FILE looks good."
-exit 0
\ No newline at end of file
+exit 0
diff --git a/scripts/fix-env.sh b/scripts/fix-env.sh
new file mode 100755
index 0000000..082a92a
--- /dev/null
+++ b/scripts/fix-env.sh
@@ -0,0 +1,33 @@
+#!/usr/bin/env bash
+# 一键修复 docker-compose.env 中常见的“复制粘贴/符号”问题
+# - 去掉反引号/全角反引号
+# - 去掉 API_URL/LIVEKIT_URL/MINIO_URL 的首尾引号
+# - 修正常见的 “URL 被包裹成 `https://...` / 'https://...' / \"https://...\" ”
+set -euo pipefail
+
+ENV_FILE="${1:-docker-compose.env}"
+
+if [ ! -f "$ENV_FILE" ]; then
+  echo "❌ fix-env: $ENV_FILE not found"
+  exit 1
+fi
+
+echo "✅ fix-env: cleaning $ENV_FILE ..."
+
+# 1) 去掉反引号
+perl -pi -e "s/\\x60//g; s/｀//g" "$ENV_FILE"
+
+# 2) 去掉关键字段的首尾引号（只处理成对的单引号/双引号）
+strip_wrap() {
+  local key="$1"
+  # 使用 perl 精准替换：KEY="xxx" -> KEY=xxx；KEY='xxx' -> KEY=xxx
+  perl -pi -e "s/^(${key})=\\\"(.*)\\\"\\s*\$/\\1=\\2/; s/^(${key})='(.*)'\\s*\$/\\1=\\2/" "$ENV_FILE"
+}
+
+strip_wrap "API_URL"
+strip_wrap "LIVEKIT_URL"
+strip_wrap "MINIO_URL"
+
+echo "✅ fix-env: done."
+echo "   Next: bash scripts/env-lint.sh $ENV_FILE"
+
diff --git a/scripts/health-check.sh b/scripts/health-check.sh
index d504cbf..a332819 100755
--- a/scripts/health-check.sh
+++ b/scripts/health-check.sh
@@ -22,6 +22,13 @@ if [ -d "$PROJECT_DIR" ]; then
 fi
 echo

+#
+# NOTE:
+# - 强制重建容器后，Traefik/后端服务会有一个短暂启动期；
+# - 探活太早可能出现 404/502（通常等待几十秒后会恢复）。
+#
+sleep "${SLEEP_BEFORE_CHECK:-6}"
+
 echo "=== ENV LINT ==="
 if [ -d "$PROJECT_DIR" ] && [ -f "$PROJECT_DIR/scripts/env-lint.sh" ]; then
   (cd "$PROJECT_DIR" && bash scripts/env-lint.sh "$ENV_FILE") || true
@@ -52,7 +59,29 @@ wait_http() {
   return 1
 }

+wait_traefik_router() {
+  local router_name="$1"     # e.g. api-gw@docker
+  local tries="${2:-30}"
+  local sleep_s="${3:-1}"
+
+  local i ok
+  for i in $(seq 1 "$tries"); do
+    ok="$(curl -sS --max-time 2 "http://127.0.0.1:11001/api/http/routers" 2>/dev/null \
+      | grep -F "\"name\":\"$router_name\"" || true)"
+    if [ -n "$ok" ]; then
+      echo "✅ traefik router ready: $router_name"
+      return 0
+    fi
+    echo "⏳ [$i/$tries] waiting traefik router: $router_name"
+    sleep "$sleep_s"
+  done
+  echo "❌ traefik router not found: $router_name" >&2
+  return 1
+}
+
 echo "=== HEALTHCHECK (internal first; bypass nginx) ==="
+wait_traefik_router "api-gw@docker" 30 1 || true
+wait_traefik_router "admin@docker" 30 1 || true
 wait_http "http://127.0.0.1:11000/health" "^(200|204)$" 60 2 || true
 wait_http "http://127.0.0.1:11000/" "^(200|301|302|401|403)$" 60 2 || true
 wait_http "http://127.0.0.1:11000/admin/" "^(200|301|302|401|403)$" 60 2 || true
diff --git a/scripts/pull-up.sh b/scripts/pull-up.sh
index daedde3..51acd1d 100755
--- a/scripts/pull-up.sh
+++ b/scripts/pull-up.sh
@@ -25,6 +25,17 @@ echo "========================================="
 echo "✅ Update Successful!"
 echo "Waiting services to be ready..."

+#
+# NOTE:
+# - 首次拉起/强制重建后，Traefik 需要一点时间从 Docker 读取 labels 并生成路由；
+# - service-core 也需要时间完成启动并开始监听 3000 端口；
+# - 若探活太早，可能出现短暂的 404/502（并不代表最终启动失败）。
+#
+# 可通过环境变量调整等待时间：
+#   SLEEP_BEFORE_CHECK=10 IMAGE_TAG=xxxx bash scripts/pull-up.sh
+#
+sleep "${SLEEP_BEFORE_CHECK:-8}"
+
 wait_http() {
   local url="$1"
   local ok_re="$2"
@@ -44,6 +55,35 @@ wait_http() {
   return 1
 }

+# 等待 Traefik 加载到关键路由（避免“路由尚未加载完成”导致的误判）
+wait_traefik_router() {
+  local router_name="$1"     # e.g. api-gw@docker
+  local tries="${2:-30}"     # 30 * 1s = 30s
+  local sleep_s="${3:-1}"
+
+  local i ok
+  for i in $(seq 1 "$tries"); do
+    ok="$(curl -sS --max-time 2 "http://127.0.0.1:11001/api/http/routers" 2>/dev/null \
+      | grep -F "\"name\":\"$router_name\"" || true)"
+    if [ -n "$ok" ]; then
+      echo "✅ traefik router ready: $router_name"
+      return 0
+    fi
+    echo "⏳ [$i/$tries] waiting traefik router: $router_name"
+    sleep "$sleep_s"
+  done
+
+  echo "❌ traefik router not found: $router_name" >&2
+  echo "   Hint: check traefik routers and logs:" >&2
+  echo "     curl -s http://127.0.0.1:11001/api/http/routers | head -n 200" >&2
+  echo "     docker logs --tail=200 tailchat-source-traefik-1" >&2
+  return 1
+}
+
+# 先等路由加载完成（即使失败也不阻断后续探活输出，便于定位）
+wait_traefik_router "api-gw@docker" 30 1 || true
+wait_traefik_router "admin@docker" 30 1 || true
+
 # 内部探活（绕过 nginx，避免外网 502 误判）
 wait_http "http://127.0.0.1:11000/health" "^(200|204)$" 60 2 || true
 wait_http "http://127.0.0.1:11000/" "^(200|301|302|401|403)$" 60 2 || true
diff --git a/server/mixins/socketio.mixin.ts b/server/mixins/socketio.mixin.ts
index ebb9c24..8c3769e 100644
--- a/server/mixins/socketio.mixin.ts
+++ b/server/mixins/socketio.mixin.ts
@@ -571,11 +571,33 @@ export const TcSocketIOService = (
             'Need to use with [ApiGatewayMixin]'
           );
         }
+
+        /**
+         * 生产环境收敛 Socket.IO CORS：
+         * - 默认仅允许主站域名（避免被第三方站点滥用发起连接）
+         * - 可通过 SOCKETIO_CORS_ORIGINS 逗号分隔扩展
+         * - 开发环境默认允许 '*'
+         */
+        const originsEnv = process.env.SOCKETIO_CORS_ORIGINS;
+        const defaultOrigins =
+          process.env.NODE_ENV === 'production' ? ['https://goodspage.cn'] : ['*'];
+        const allowedOrigins = originsEnv
+          ? originsEnv
+              .split(',')
+              .map((s) => s.trim())
+              .filter(Boolean)
+          : defaultOrigins;
+
+        const origin =
+          allowedOrigins.includes('*')
+            ? '*'
+            : (allowedOrigins as unknown as string[]);
+
         this.io = new SocketServer(this.server, {
           serveClient: false,
           transports: ['websocket'],
           cors: {
-            origin: '*',
+            origin,
             methods: ['GET', 'POST'],
           },
           parser: options.disableMsgpack ? undefined : msgpackParser,
diff --git a/server/packages/sdk/src/index.ts b/server/packages/sdk/src/index.ts
index 9ef81b2..114a681 100644
--- a/server/packages/sdk/src/index.ts
+++ b/server/packages/sdk/src/index.ts
@@ -62,7 +62,26 @@ export type { BrokerOptions } from 'moleculer';
  */
 process.on('unhandledRejection', (reason, promise) => {
   console.error('unhandledRejection', reason);
+
+  const shouldExit =
+    process.env.EXIT_ON_UNCAUGHT !== undefined
+      ? process.env.EXIT_ON_UNCAUGHT === '1' || process.env.EXIT_ON_UNCAUGHT === 'true'
+      : process.env.NODE_ENV === 'production';
+
+  if (shouldExit) {
+    // 给日志一点刷新时间，然后交给容器自愈重启
+    setTimeout(() => process.exit(1), 200);
+  }
 });
 process.on('uncaughtException', (error, origin) => {
   console.error('uncaughtException', error);
+
+  const shouldExit =
+    process.env.EXIT_ON_UNCAUGHT !== undefined
+      ? process.env.EXIT_ON_UNCAUGHT === '1' || process.env.EXIT_ON_UNCAUGHT === 'true'
+      : process.env.NODE_ENV === 'production';
+
+  if (shouldExit) {
+    setTimeout(() => process.exit(1), 200);
+  }
 });
diff --git a/server/plugins/com.ridou.defense-control/executor/executor.js b/server/plugins/com.ridou.defense-control/executor/executor.js
index 45f9222..11de79e 100644
--- a/server/plugins/com.ridou.defense-control/executor/executor.js
+++ b/server/plugins/com.ridou.defense-control/executor/executor.js
@@ -4,7 +4,8 @@ const https = require('https');
 const { execSync } = require('child_process');

 const PORT = process.env.EXECUTOR_PORT || 9099;
-const SHARED_SECRET = process.env.DEFENSE_SHARED_SECRET || 'defense-secret-key';
+// 生产环境必须显式配置，否则签名校验形同虚设
+const SHARED_SECRET = process.env.DEFENSE_SHARED_SECRET;

 const STATES = {
   IDLE: 'IDLE',
@@ -19,6 +20,9 @@ let currentState = STATES.IDLE;
 let currentConfig = {};

 function verifySignature(rawBody, signature) {
+  if (!SHARED_SECRET) {
+    return false;
+  }
   const hmac = crypto.createHmac('sha256', SHARED_SECRET);
   hmac.update(rawBody);
   const expected = hmac.digest('hex');
diff --git a/server/plugins/com.ridou.defense-control/services/defense.service.ts b/server/plugins/com.ridou.defense-control/services/defense.service.ts
index 4c8a7d4..a78f008 100644
--- a/server/plugins/com.ridou.defense-control/services/defense.service.ts
+++ b/server/plugins/com.ridou.defense-control/services/defense.service.ts
@@ -5,9 +5,15 @@ import crypto from 'crypto';
 import http from 'http';

 const EXECUTOR_PORT = process.env.EXECUTOR_PORT || 9099;
-const SHARED_SECRET = process.env.DEFENSE_SHARED_SECRET || 'defense-secret-key';
+// 生产环境必须显式配置，否则 HMAC 校验形同虚设
+const SHARED_SECRET = process.env.DEFENSE_SHARED_SECRET;

 async function callExecutor(toState: string, config: any): Promise<any> {
+  if (!SHARED_SECRET) {
+    throw new Error(
+      'DEFENSE_SHARED_SECRET is not set. Refuse to call executor in production mode.'
+    );
+  }
   return new Promise((resolve, reject) => {
     const body = JSON.stringify({ toState, config });
     const hmac = crypto.createHmac('sha256', SHARED_SECRET);
@@ -57,6 +63,13 @@ class DefenseControlService extends TcService {
   }

   onInit() {
+    if (!process.env.DEFENSE_SHARED_SECRET) {
+      this.logger.warn(
+        '[defense-control] DEFENSE_SHARED_SECRET is not configured. ' +
+          'Defense executor integration will be disabled until you set it.'
+      );
+    }
+
     this.registerLocalDb(require('../models/defense').default);
     this.registerLocalDb(require('../models/defense').DefenseAuditLogModel);

diff --git a/server/services/openapi/oidc/oidc.service.ts b/server/services/openapi/oidc/oidc.service.ts
index f5899e2..050683c 100644
--- a/server/services/openapi/oidc/oidc.service.ts
+++ b/server/services/openapi/oidc/oidc.service.ts
@@ -14,6 +14,10 @@ import type { UserLoginRes } from '../../../models/user/user';
 const PORT = process.env.OPENAPI_PORT || config.port + 1;
 const ISSUER = config.apiUrl;
 const IS_PROXY = process.env.OPENAPI_UNDER_PROXY === 'true';
+const REQUIRE_PKCE =
+  process.env.OIDC_REQUIRE_PKCE !== undefined
+    ? process.env.OIDC_REQUIRE_PKCE === '1' || process.env.OIDC_REQUIRE_PKCE === 'true'
+    : process.env.NODE_ENV === 'production';

 const configuration: Configuration = {
   adapter: TcOIDCAdapter,
@@ -21,7 +25,7 @@ const configuration: Configuration = {
   clients: [],
   pkce: {
     methods: ['S256'],
-    required: () => false, // TODO: false in test
+    required: () => REQUIRE_PKCE,
   },
   claims: {
     profile: ['nickname', 'discriminator', 'avatar'],
