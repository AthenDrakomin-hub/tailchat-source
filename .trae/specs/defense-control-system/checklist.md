- [x] L0: 插件可在管理端一键开关禁注册/只读/限上传，并能即时生效或恢复，附带审计记录。
- [x] L1: 管理面板提供基础限流配置并能传递给控制器，生成对应的 Nginx 限流配置建议或应用。
- [x] L2: 配置缺失时（如 CF 凭证不全），插件拒绝切换（展示 BLOCK 状态）。
- [x] L2: 切档过程中，执行器能够正确运行 PRECHECK 和 DRYRUN 阶段，并在探活失败时中止切换。
- [x] L2: COMMIT 成功后，源站公网只允许 Cloudflare IP 回源，直连扫描业务端口被拒绝，且 LiveKit WS 可正常连通。
- [x] 自动回滚: 若切档后持续 3 次探活失败（例如 502/连接拒绝），控制器能自动恢复防火墙规则至切档前快照，并锁定 10 分钟。
- [x] L3: 支持在插件配置边缘节点，且能支持一键回滚到 L2。
- [x] 审计日志: 切档成功/失败/回滚的操作都能被记录并在 Admin 面板可见。