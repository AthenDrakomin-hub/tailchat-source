- [x] 组织代码的配置项已经能够在管理后台（Admin Panel）的系统设置中展示和修改。
- [x] 用户能够通过配置的组织代码（例如 `0501`）顺利完成注册。
- [x] LiveKit 的 `generateGuestToken` 后端接口能够在没有认证请求头的情况下正常响应并返回 AccessToken。
- [x] `/plugin/com.msgbyte.livekit/meeting/:meetingId` 路由能够正常加载全屏的外部语音会议界面。
- [x] 未登录的访客在进入外部语音会议前，页面会要求其输入昵称（Guest Nickname）。
- [x] 输入昵称后的访客能够成功连接到对应的 LiveKit 会议室并参与语音通话。
- [x] 现有的已登录用户在内部发起语音通话时不受影响，正常获取 Token 并连接。
- [x] 在会议内的控制栏（ControlBar）中点击“分享链接”按钮，能够正确复制会议的外部访问链接到剪贴板，并弹出提示。
