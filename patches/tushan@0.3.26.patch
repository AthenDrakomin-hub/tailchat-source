diff --git a/client/components/edit/EditForm.tsx b/client/components/edit/EditForm.tsx
index 1111111..2222222 100644
--- a/client/components/edit/EditForm.tsx
+++ b/client/components/edit/EditForm.tsx
@@
 import { useSendRequest } from '../../hooks/useSendRequest';
 import type { FieldHandler } from '../fields/factory';
 import { SubmitButton } from '../SubmitButton';
+
+function getErrorMessage(err: unknown) {
+  if (err instanceof Error && err.message) {
+    const body = (err as any).body;
+    if (body && typeof body.error === 'string' && body.error) {
+      return body.error;
+    }
+
+    return err.message;
+  }
+
+  if (err && typeof err === 'object') {
+    const body = (err as any).body;
+    if (body && typeof body.error === 'string' && body.error) {
+      return body.error;
+    }
+    if (typeof (err as any).message === 'string' && (err as any).message) {
+      return (err as any).message;
+    }
+  }
+
+  return String(err);
+}
@@
     } catch (err) {
-      Message.error(t('tushan.common.operateFailed') + ':' + String(err));
+      Message.error(
+        t('tushan.common.operateFailed') + ':' + getErrorMessage(err)
+      );
     }
   });
diff --git a/client/hooks/useSendRequest.ts b/client/hooks/useSendRequest.ts
index 3333333..4444444 100644
--- a/client/hooks/useSendRequest.ts
+++ b/client/hooks/useSendRequest.ts
@@
 import { Message } from '@arco-design/web-react';
 import { useEvent } from './useEvent';
+
+function getErrorMessage(err: unknown) {
+  if (err instanceof Error && err.message) {
+    const body = (err as any).body;
+    if (body && typeof body.error === 'string' && body.error) {
+      return body.error;
+    }
+
+    return err.message;
+  }
+
+  if (err && typeof err === 'object') {
+    const body = (err as any).body;
+    if (body && typeof body.error === 'string' && body.error) {
+      return body.error;
+    }
+    if (typeof (err as any).message === 'string' && (err as any).message) {
+      return (err as any).message;
+    }
+  }
+
+  return String(err);
+}
@@
   return useEvent(async (...args: T) => {
     try {
       await fn(...args);
     } catch (err) {
-      Message.error(String(err));
+      Message.error(getErrorMessage(err));
     }
   });
 }
