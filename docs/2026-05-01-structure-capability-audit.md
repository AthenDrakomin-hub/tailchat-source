# Tailchat Source 结构与能力审计

## 2026-05-02 財訊品牌與信任展示完成状态

在本轮“財訊”品牌升级执行后，仓库状态新增了以下真实变化：

- 客户端入口层已经不再只是历史 Tailchat 壳，而是形成了以 `財訊` 为唯一对外产品名的正式展示层
- 登录、注册、访客入口、找回密码、设置中的关于与信任入口，以及正式的关于我们、用戶協議、隱私政策、社區公約、安全與合規页面，已经构成可访问的品牌与信任闭环
- favicon、PWA 图标、Open Graph 分享图、客户端标题与官网首页已经同步承接 `財訊 / 日斗投資財富論壇 / 第十屆交流會 / 日斗投資諮詢有限公司` 这四层身份信息
- 本轮仍然遵循“展示层先更名、技术层暂缓物理 rename”的策略，因此没有为了品牌升级而对仓库名、包名和底层插件标识进行高风险整体替换
- 当前仓库已经从“只有产品体验收口”进一步推进到“客户端品牌感、信任感、合规展示可见层已基本成型”的状态

## 2026-05-02 財訊 IM Phase 1 完成状态

在本轮继续推进后，仓库已不再停留在“品牌层升级”，而是开始进入真正的 `財訊 IM` 产品骨架阶段：

- 主导航已经新增 `动态` 一级入口，产品从“聊天 / 联系人 / 群组 / 我的”继续扩展为更符合财富论坛生态的五入口结构
- 动态系统已经以 MVP 方式落地到客户端与服务端，支持发布动态、查看动态流、点赞以及与群组关联
- 群组侧栏已经出现“关联动态”承接入口，聊天、群组、动态三大主场开始发生基础联动
- 服务端已经新增 `openapi.account` 标准化账号控制 facade，外部模型后续可以在统一入口上逐步获得会话、群组、资料、动态等账号控制能力
- 当前阶段仍然属于 Phase 1 基础成型：重点是熟悉型 IM 骨架、财富论坛动态主场和账号控制底座，不是一次性完成完整自动化运营系统

## 2026-05-02 財訊 IM Phase 2 主场联动进展

在第二阶段开始后，仓库已经从“动态入口可用”推进到“动态、群组、账号控制层开始形成联动闭环”：

- 动态系统已补充动态详情页、评论区和个人动态页，不再只是单页动态流
- 动态卡片已开始承担作者主页入口、关联群组入口与内容详情入口
- 群组侧栏中的“关联动态”不再只是单个链接，而是开始承担活动预热、观点沉淀与公开导流说明
- `openapi.account` 已新增获取动态详情、获取个人动态、评论动态、删除动态、获取群详情、更新群公告等能力
- 当前仓库已开始形成“动态发布 -> 群组承接 -> 统一账号控制可操作”的主链路基础

## 2026-05-02 財訊 IM Phase 3 运营链路强化进展

第三阶段继续沿着“内容场 -> 群承接 -> 账号读取上下文 -> 连续动作”推进，当前仓库新增了更接近长期运营所需的能力：

- 动态页已开始支持“全部动态 / 我的动态”视图切换，内容场不再只有单一公共流
- 动态详情页已补充互动状态展示，评论新增后会即时反馈到详情页内容状态
- 群头部开始直接承接群说明 / 公告摘要，用户进入群时更容易理解当前群定位和活动状态
- `openapi.account` 已继续扩展：可读取动态评论、点赞动态、读取群成员、读取会话详情和会话消息列表
- 当前仓库已更接近“角色账号能够读取上下文、判断状态并持续运营动作”的基础平台，而不只是单向发内容

## 2026-05-02 財訊 IM Phase 4 群运营上下文进展

第四阶段开始将“群”作为更明确的运营主场继续补强，当前仓库新增了以下能力：

- `openapi.account` 已可显式读取群公告、群大厅会话标识和群最近消息，不再需要调用方自行拼接多个内部接口
- 动态详情页里的关联群承接不再只是单个链接，而是会展示群名称、成员数量、群说明摘要和进入群组入口
- 当前产品链路已更接近“看到内容 -> 判断群状态 -> 决定是否进入群参与”的真实运营路径

## 2026-05-02 財訊 IM Phase 5 群大厅联动进展

第五阶段继续把“群大厅”从隐性能力推进成显性承接节点，当前仓库新增了以下变化：

- `openapi.account` 已支持基于 `groupId` 直接向群大厅发消息，调用方不再需要自行解析大厅会话后再发言
- 动态详情页中的关联群摘要卡片已开始展示群最近消息预览，用户在内容场中即可感知当前群交流氛围
- 当前平台已更接近“看内容 -> 看群氛围 -> 进入群 / 直接围绕群大厅发起运营动作”的连续链路

## 2026-05-02 財訊 IM Phase 6 群运营上下文打包进展

第六阶段继续把“群上下文”从零散读取动作推进到更适合运营账号直接消费的形态：

- `openapi.account` 已新增 `getGroupOperationsContext`，可一次性返回群详情、公告、群大厅会话、成员列表和最近消息
- 动态详情中的关联群卡片已开始展示最近消息发送者和时间，不再只是孤立文本预览
- 动态详情中的进群动作提示已更明确，当前产品更接近“内容判断 -> 群氛围判断 -> 参与讨论”的连续路径

## 2026-05-02 財訊 IM Phase 7 群运营摘要进展

第七阶段继续把群上下文从“可读原始数据”推进到“可直接用于判断的摘要数据”：

- `openapi.account` 已新增 `getGroupOperationsSummary`，可直接返回成员数量、最近消息数量、最近活跃时间等摘要字段
- 动态详情中的关联群卡片已开始显式展示最近活跃时间，使用户更容易判断当前群是否仍在活跃交流
- 当前产品链路已经更接近“看内容 -> 判断群活跃度 -> 决定进入讨论”的轻决策路径

## 2026-05-02 財訊 IM 可内测里程碑完成状态

在完成前七轮连续增量后，仓库已进一步收口到“可内测”阶段，新增的关键变化包括：

- `openapi.account.getGroupOperationsSummary` 已补充 `hasRecentMessages`、`isRecentlyActive`、`activityStatus` 等轻信号字段
- `openapi.account.sendFeedRelatedGroupMessage` 已打通“动态 -> 关联群大厅”的最小连续动作闭环
- 消息列表已开始对明显的官方 / 服务 / 活动型会话做轻量角色识别
- 动态详情中的关联群卡片已进一步收口为更接近成品的承接视图，能够提示当前群是否适合立即进入讨论


## 2026-05-02 客户端可用性里程碑进展

在确认下一阶段核心任务应转向客户端后，仓库已开始从“链路成立”推进到“用户入口更像真实客户端”：

- `client/web` 登录入口已增加多端导流与内测阶段说明，Web 主入口定位更清楚
- `client/desktop` 启动器已从通用服务器选择壳收口为更明确的財訊桌面客户端入口
- `client/mobile` 入口页已从技术性服务器选择页收口为更接近真实移动客户端的入口层
- `website/downloads` 已开始承担更可信的客户端分发说明，能清楚表达 Web / Desktop / Mobile 当前阶段与推荐使用方式

这意味着当前项目已不再只是在做服务端能力铺设，而是开始真正围绕终端用户可见的客户端体验推进。

## 2026-05-02 客户端主路径收口进展

在完成客户端入口层收口后，当前仓库继续往“进入后更像真实客户端”推进：

- `client/web` 的动态首页、群组总览和提醒页未选择状态已不再只是简短占位，而是开始承担第一屏引导职责
- `client/mobile` 的 `WebView` 主内容已增加原生顶部栏，能够显示当前工作区、切换工作区并刷新页面
- 当前客户端策略已从“让用户知道怎么进入”继续推进到“让用户进入后知道下一步做什么”

这一步对于后续把 Web 打磨成熟、并把移动端逐渐推进到可上架路径，是必要的中间阶段。

## 2026-05-02 会话主路径与移动壳层优化进展

在继续沿客户端优先路线推进后，当前仓库又进一步补上了两类高频使用体验：

- `client/web` 的个人消息侧栏在最近聊天为空时，已开始给出明确的“如何开始私信”的引导
- `client/web` 的联系人页已补充更像成品的顶部说明区域，让“联系人 -> 私信”路径更容易理解
- `client/mobile` 的主内容容器已补充返回、加载反馈、错误提示和重试动作，移动端 WebView 外壳不再只是最低限度包裹

这意味着当前客户端优化已经从入口层和第一屏，继续推进到实际高频使用路径的可理解性与反馈完整度。

## 2026-05-02 聊天主场与弱网反馈收口进展

在继续围绕高频客户端使用体验推进后，当前仓库又补上了两类关键细节：

- `client/web` 的空会话聊天面板已不再只有一句占位，而是开始提示如何发起第一条消息、如何明确话题以及如何沉淀后续沟通
- `client/mobile` 的主内容壳层已开始对慢网络和 HTTP 异常提供显式提示，不再只有笼统的加载或失败状态

这一步意味着客户端优化已经开始覆盖更贴近真实用户感知的“空会话起点”和“弱网使用反馈”场景。

## 2026-05-02 聊天主场信息层级进展

在继续围绕长期使用感推进后，当前仓库又补上了两类更细的聊天体验：

- `client/web` 的私信会话头部已不再只有标题，而是开始说明一对一沟通的典型用途与场景
- `client/mobile` 的当前工作区壳层已开始明确区分“正在连接”“正在同步”“服务响应异常”“内容已恢复”等状态

这意味着当前客户端优化已经从“能进入、能使用”继续推进到“聊天场景的信息层级更清晰、状态反馈更可感知”的阶段。

## 2026-05-02 Desktop 连续体验进展

在开始把桌面端从“有启动器”推进到“进入过程更像真实客户端”之后，当前仓库又补上了两类关键体验：

- `client/desktop` 在点击工作区后，已开始显式显示“正在进入工作区”的连接态，并收紧其他操作，避免用户误以为点击无效
- `client/web` 的私信会话头部已继续加强“私密会话 / 长期跟进”等语义，使聊天主场更像长期使用中的沟通面板

这意味着客户端优化已经不再只聚焦 Web 与 Mobile，也开始把 Desktop 的连续使用感纳入主线。

## 2026-05-02 Desktop 主窗加载反馈进展

在继续沿桌面端长期使用感推进后，当前仓库又补上了两类更贴近主窗使用过程的体验：

- `client/desktop` 的主窗口在加载工作区时，已开始通过窗口标题和进度状态明确表达“正在连接 / 正在同步 / 已就绪 / 加载失败”
- `client/web` 的聊天输入区已补充更明确的底部提示栏，使输入区更像长期沟通工具而不只是一个发送框

这意味着客户端优化已经从“启动器体验”继续推进到“真正进入主内容后的连续反馈”阶段。

## 2026-05-02 Desktop 恢复与 Mobile 刷新一致性进展

在继续围绕“失败后怎么办”和“恢复是否一致”推进后，当前仓库又补上了两类关键体验：

- `client/desktop` 的主窗在工作区加载失败时，已开始提供明确的“重试”与“返回工作区选择”恢复动作
- `client/mobile` 的当前工作区容器已把刷新与重试统一为同一套恢复流程，避免不同入口下的状态反馈不一致

这意味着客户端优化已经开始进入更成熟的恢复链路阶段，而不再只是单纯展示状态。

## 2026-05-02 Desktop 主动返回与 Mobile 恢复完成反馈进展

在继续围绕连续恢复体验推进后，当前仓库又补上了两类更贴近产品感的动作：

- `client/desktop` 已开始支持从主窗主动返回工作区选择，而不必等到错误时再被动回退
- `client/mobile` 在刷新或重试成功后，已开始短暂展示“已恢复当前工作区内容”的完成反馈

这意味着客户端优化已经从“有恢复能力”继续推进到“恢复动作更主动、恢复结果更可感知”的阶段。

## 2026-05-02 最终恢复体验收口进展

在继续做最后一轮客户端恢复体验收口后，当前仓库又补上了两类更贴近长期使用感的细节：

- `client/desktop` 的主窗已不只是支持返回工作区选择，还开始提供“在浏览器打开当前工作区”“复制当前工作区地址”等当前工作区操作
- `client/mobile` 的恢复成功提示已不再和首次正常加载混在一起，而是只在用户主动刷新或重试成功后短暂出现

这意味着客户端恢复链路已经从“有动作”进一步推进到“动作更完整、提示更准确”的阶段。

## 2026-05-01 第三阶段完成状态

在第三阶段执行完成后，当前仓库状态已进一步发生变化：

- 客户端个人区的主路径已经从旧的 `converse` 语义继续收口为 `chats`，且主客户端与面板层都保留了旧路径兼容，不再处于“入口改了一半”的状态
- 联系人、聊天、群组和通知这四条最核心的用户主路径已经完成连续多轮展示层和交互层统一，明显减少了旧词汇、旧块状风格与新路径并存的割裂感
- LiveKit 相关通话链路虽然底层仍是插件架构，但预加入、通话主界面、侧栏、控制栏、邀请通知、外部接听页和访客接听页都已继续弱化默认会议插件感，更接近产品内通话体验
- 本轮不是简单叠加新文件，而是以迭代修改现有源文件为主，仅新增了必要的路径工具和阶段计划文档；同时已经删除无调用的新辅助函数，避免冗余残留
- 经过第三阶段收尾后，仓库已经从“仍在明显产品改造中”推进到“主路径、主语义、主通话体验已基本成型，可作为一个干净、可编译、可继续维护的版本基线”

## 2026-05-01 第二阶段完成状态

在本轮第二阶段执行后，以下结论已发生变化：

- Web 主客户端的信息架构、主导航语义和侧栏层级已明显收口，不再处于原先的“入口混杂、路径感不清”状态
- Mobile 仍然是 WebView 壳，但其关键窄屏爆点已经经过多轮约束修补，不再维持原始溢出状态
- Admin 仍然依赖多条底层链路，但高风险页面已经补上更明确的降级态与操作反馈，不再只是“页面存在但炸了就白屏/碎错误”
- 音视频能力底层仍来自 LiveKit 插件体系，但私聊通话默认体验已经从“独立窗口 / 会议感”收口到更接近应用内会话通话
- 第一批视觉统一已完成，主导航、侧栏、通知列表、群组列表、页头与空状态已经进入同一套产品语言

因此，当前仓库的真实状态已经从“需要先做结构审计才能判断是否能改”推进到“第二阶段基础产品收口已完成，可进入下一阶段更深层产品与能力重构”。

## 审计目的

本报告只做一件事：

- 把当前仓库真实结构、运行边界、功能能力边界和明显缺口讲清楚

不再以“服务能启动”“接口返回 200”替代“产品真的可用”。

## 当前仓库结论

当前仓库不是一个“已经打磨完成、可直接承接完整社群运营生态”的成熟产品，而是：

- 一个可二次开发的通讯底座
- 带有 Web、Mobile、Admin、Plugin、Deploy 等多层结构
- 但产品层体验、移动端体验、管理端稳定性、音视频产品定义仍未达到可直接承载高强度运营的程度

## 一、仓库结构总览

### 1. 顶层工作区

根工作区 `package.json` 说明：

- 统一开发入口：
  - `pnpm dev`
  - `pnpm dev:admin`
- 统一构建入口：
  - `pnpm build`

说明这不是单体项目，而是一个前后端 + 多客户端 + 插件的 monorepo。

### 2. 主要目录

- `client/web`
  - Web 主客户端
- `client/mobile`
  - 移动端壳
- `client/desktop`
  - 桌面端
- `server`
  - 服务端主程序、插件、管理后台
- `server/admin`
  - 管理后台独立前后端
- `server/plugins`
  - 功能插件
- `docker-compose.yml`
  - 生产部署主入口
- `scripts/`
  - 部署、校验、重置等脚本

## 二、运行形态审计

### 1. 生产部署形态

当前生产部署核心形态来自 `docker-compose.yml`：

- `service-core`
- `service-openapi`
- `service-all-plugins`
- `tailchat-admin`
- `mongo`
- `redis`
- `minio`
- `livekit`
- `traefik`

这说明线上不是简单单体服务，而是：

- 核心服务 + 插件服务 + 管理后台 + 基础设施拼装式架构

### 2. Admin 不是独立 SaaS 控制台

`server/admin/src/client/App.tsx` 显示：

- Admin 前端使用 `tushan`
- 数据来源主要是 `/admin/api`
- 页面本质上是多组 `CustomRoute + Resource`

而 `server/admin/src/server/index.ts` 与 `server/admin/src/server/router/*.ts` 显示：

- Admin 后端只是一个附属 Express 服务
- 既依赖数据库，也依赖 broker，也依赖部分外部执行器

这意味着：

- Admin 很容易出现“页面存在，但底层依赖链没通时直接 500/503”的情况

## 三、移动端审计

### 1. 移动端不是独立原生 IM

`client/mobile/src/AppMain.tsx` 明确显示：

- 移动端核心界面来自 `react-native-webview`
- `source={{ uri: props.host }}`
- 注释明确写了：
  - “由 webview 提供”

结论：

- 当前移动端本质是“网页壳”
- 不是独立实现的原生消息界面
- Web 端导航复杂、布局不适配、信息层级不清晰的问题，会原样投射到移动端

### 2. 对移动端体验的直接影响

这能直接解释你提到的现象：

- 手机上找消息入口很费劲
- 内容边界出界
- 不像一个原生通讯产品

这不是单一 CSS 小 bug，而是产品形态决定的：

- Mobile 当前更像 Web 包壳，不是单独为移动使用场景设计的消息产品

## 四、消息与好友能力审计

### 1. 好友消息入口确实不够直接

`client/web/src/routes/Main/Content/Personal/Sidebar.tsx` 显示：

- 好友入口在：
  - `/main/personal/friends`
- 私信入口在：
  - `PersonalSidebar` 的 “私信” 分区
- DM 列表由：
  - `useDMConverseList()`

这意味着当前信息架构是：

- 个人页
  - 好友
  - 私信
  - 插件面板

而不是很多成熟 IM 常见的：

- 一个清晰统一的“聊天/会话”主入口
- 好友、群聊、私聊在一个明确主导航下整合

### 2. 好友发消息能力是有的，但入口不够产品化

`client/web/src/routes/Main/Content/Personal/Friends/FriendList.tsx` 显示：

- 每个好友条目有“发送消息”按钮
- 点击后走：
  - `createDMConverse([targetId])`
  - 然后 `navigate(/main/personal/converse/:id)`

所以结论不是“没有私聊能力”，而是：

- 私聊能力存在
- 但入口组织和路径表达不够清晰
- 对新用户来说不符合“打开就能明白怎么找好友消息”的预期

## 五、语音/音视频能力审计

### 1. 当前实现不是纯粹的好友直达通话模型

从 `client/web/src/plugin/builtin.ts` 和 `server/plugins/com.msgbyte.livekit/...` 可以确认：

- 音视频能力来自 `com.msgbyte.livekit`
- 插件关键词明确是：
  - `meeting`
  - `live streaming`
  - `voice channel`
  - `guest-call`
  - `meeting/:meetingId`
  - `shortlink`

### 2. 这和“即时私聊语音”不是一回事

它更接近：

- 房间 / 会议模型
- 插件式 meeting room
- 邀请 / guest-call / roomName 机制

而不是用户直觉中的：

- 打开好友资料
- 直接点语音
- 立刻进入点对点通话

### 3. 这解释了你的强烈不适感

你说的这个感受是合理的：

- 看起来不像正常即时通讯中的好友语音
- 反而像借助外部会议链接或房间机制做的功能拼装

结论：

- 当前音视频能力存在
- 但产品模型偏“插件式会议”
- 不等于成熟 IM 的“好友直达语音通话”

## 六、管理端能力审计

### 1. 管理端页面很多，但依赖链很长

`server/admin/src/client/App.tsx` 里挂了这些页面：

- `ops-control`
- `defense-control`
- `network`
- `socketio-diagnostic`
- `analytics`
- `cache`
- `system-notify`
- `site-config`
- `users`
- `messages`
- `groups`
- `file`
- `mail`
- `plugin-registry`

### 2. 页面并不等于可用

例如：

- `network` 依赖 broker 注册表
- `ops` 依赖：
  - `OPS_EXECUTOR_URL`
  - `EXECUTOR_SHARED_SECRET`
  - `config.get/config.set`
- `analytics` 依赖数据库聚合
- 资源页依赖：
  - Mongo 查询
  - broker action
  - `raExpressMongoose`

所以当前 Admin 的真实风险不是“有没有页面”，而是：

- 页面多
- 依赖多
- 任一底层链条不稳，就会 500/503
- 容易形成“登录页正常，进去就炸”的体验

### 3. Admin 当前真实状态判断

当前可以初步判定：

- Admin 是“结构上完整”
- 但不是“能力上闭环”
- 它更像一组系统运维/数据面板的聚合，而不是一个经过严格稳定性验收的后台产品

## 七、群组内容与公告能力审计

### 1. 群组基础说明不是 HTML 公告编辑器

群组基础说明 `description`：

- 只是字符串字段
- 不是正式富文本公告系统

### 2. 欢迎词也不是 HTML 富文本

`com.msgbyte.welcome` 使用普通 `TextArea`

只支持：

- 普通文本
- `{nickname}`
- `{@nickname}`

### 3. 真正适合做公告的是 Markdown Panel

群公告 / 群规则 / 说明页更接近：

- `com.msgbyte.mdpanel`

即：

- 用 Markdown，不是自由 HTML

## 八、能力矩阵

### 1. 产品基础能力

- Web 登录：可用
- Admin 登录页：可用
- 好友能力：存在，但入口组织不佳
- 私聊能力：存在，但可发现性不足
- 群消息能力：存在
- 移动端体验：不合格，当前更像 WebView 包壳
- 好友直达语音：不成立，当前更接近会议/房间插件

### 2. 管理端能力

- 页面结构：存在
- 数据依赖边界：复杂
- 系统页稳定性：高风险
- broker 强依赖页面：高风险
- executor 强依赖页面：高风险
- 是否达到“可运营后台”：未达到

### 3. 运营与生态承载能力

就你要的目标而言：

- 自动化社群运营底座：未达到
- 移动端消息产品线：未达到
- 后台稳定运营线：未达到
- 品牌/合规包装线：可以做，但现在不是最优先

## 九、最关键结论

### 1. 你失望不是因为挑剔

而是这个项目当前确实存在这些真实问题：

- 移动端体验不成立
- 私聊/消息入口不够清晰
- 语音通话产品模型不符合常规 IM 直觉
- Admin 稳定性依赖链过长，容易出现 500/503

### 2. 当前最不该继续做的事

在这些基础问题没打掉前，不应该优先做：

- 华丽包装
- 品牌视觉
- 大规模自动化运营能力叠加

否则只是在不稳底座上继续堆功能。

## 十、修复优先级建议

### 第一阶段：产品成立线

必须先解决：

1. 移动端消息入口与布局问题
2. 好友消息 / 群消息主导航重整
3. 语音通话产品定义重做
   - 从“插件会议/房间”向“好友直达语音”收敛
4. Admin 页面逐页查依赖与故障点

### 第二阶段：运营可用线

在第一阶段稳定后再做：

1. 管理端系统页统一容错
2. 群公告 / 规则 / 运营模板完善
3. 自动化运营所需后台入口

### 第三阶段：财讯包装线

最后再做：

1. 登录页与站点视觉升级
2. 合规文案与品牌表达
3. 面向业务的包装和商业呈现

## 十一、当前建议

如果继续推进，这个项目必须改打法：

- 先全仓审计
- 再按能力边界逐项打穿
- 不再把“容器启动成功”当成“项目可用”

否则继续做，只会不断追加失望。

## 十二、已开始执行的第一阶段

截至当前工作轮次，第一阶段已经开始落地：

- 一级导航中的“我”已改成“消息”
- 个人区默认落点已改成最近会话优先
- 个人区侧边栏已改为“会话优先、联系人次级”
- 主内容区和收件箱列表已开始收紧横向溢出策略

这还不是最终完成态，但已经从“只做审计”进入“按审计结论改代码”的阶段。
